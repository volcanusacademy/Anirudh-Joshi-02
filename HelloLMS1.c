#include <stdio.h>

void main() 
{
    // Display "Hello world" message on the console
    printf("Hello world\n");

    // Indicate successful program execution
    // The return value 0 is a convention for success
    // It informs the operating system that the program ran without errors
    // and has now completed
    return 0;
}
